# Pip-only fallback. The canonical install path is `conda env create -f environment.yml`,
# which pins the torch + CUDA combo that the mamba wheels were built against. If you
# install from this file directly, install torch first with the CUDA 11.8 wheel:
#   pip install torch==2.0.1 torchvision==0.15.2 --extra-index-url https://download.pytorch.org/whl/cu118
# then `pip install -r requirements.txt`.

torch==2.0.1
torchvision==0.15.2

# --- Mamba kernel: Swin-UMamba's tested combo. ---
# Newer versions are NOT drop-in compatible (causal-conv1d 1.2+ changed its
# kernel signature; mamba-ssm 2.x reorganized the public API). Stick to these
# pins unless you're prepared to verify the selective_scan_interface import
# path still works. Install causal-conv1d BEFORE mamba-ssm.
causal-conv1d==1.1.1
mamba-ssm==1.1.1

# --- model + numerics ---
einops
timm
numba
torchinfo
# numpy 2.x breaks the mamba-ssm wheel ABI; pin to the 1.x line.
numpy<2.0

# --- text encoder ---
# Pinned to the version that works with tokenizers 0.15 and our mamba wheels.
# Colab's default (5.x at the time of writing) is incompatible.
transformers==4.36.2

# --- data / augmentation ---
# Pinned to the wheel that ships with manylinux2014 (matches Colab base image).
opencv-python==4.8.1.78
albumentations
pillow

# --- training infra ---
tensorboard
matplotlib
pyyaml
tqdm

# --- caption generation (OpenAI API) ---
# Only needed for caption_gen/generate.py. Training does not import caption_gen.
openai>=1.40.0
